{
  "mcpServers": {
    "openconclave": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/../packages/server/src/mcp/server.ts"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}/.."
    }
  }
}
