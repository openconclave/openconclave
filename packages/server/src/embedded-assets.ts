// Stub for dev mode — replaced by scripts/embed-client.ts at build time
export function getAsset(_path: string): { body: Uint8Array; type: string } | null {
  return null;
}
export const hasEmbeddedAssets = false;
